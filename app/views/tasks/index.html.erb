<h1>My tasks</h1>

<% @tasks.each do |task| %>
  <%= task.completed.to_s %>
  <%= link_to task.title, task_path(task) %>
  <%= link_to "delete?", task_destroy_path(task), method: :delete %>
  <p></p>
<% end %>
<p><strong><%= link_to " + Add new task", tasks_new_path %></strong></p>
